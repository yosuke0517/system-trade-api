version: '3.7'

services:
  go:
    build:
      context: .
      target: build
    volumes:
      - ./:/go/app
    command: air
    ports:
      - 8080:8080
      - 2345:2345
    security_opt:
      - apparmor:unconfined
    cap_add:
      - SYS_PTRACE